// palettefor color
import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#000",
      light: "skyblue",
    },
    secondary: {
      main: "#15c630",
    },
    othercolor: {
      main: "#999",
    },
  },
});
